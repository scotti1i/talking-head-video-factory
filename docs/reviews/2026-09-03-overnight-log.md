# 2026-09-03 夜间闭环日志（Scott 睡了，全权授权）

目标：① 落地总审的流程加固；② 叙事舞台重剪「离职员工资料喂 AI」「AI 购物来了独立站更重要」；③ 视频生成即梦优先、APIMart 兜底，图片 Codex 优先、APIMart 兜底；④ 记事故，最后加固流程。

验收：两条片各有 1080p30 成片 + 门禁全绿 + 静帧板 + REVIEW + Downloads 交付；加固项有测试；事故日志 + 流程改动进说明书；分层 commit。

## 事故 / 阻塞记录（按时间）

（逐条追加：时间 · 现象 · 根因 · 当场处理 · 应固化为什么）

## 决策记录

- 两条片的 job：`jobs/employee-distillation-20260831`（102.5s，标题「录一遍操作，得不到一个 AI 员工」，即 Scott 说的「离职员工资料喂 AI」）、`jobs/ai-shopping-independent-site-20260831`（182.3s）。都有粗剪 A-roll（1728×3072 竖拍 30fps）。
- 时间线单源：直接对成片 A-roll 跑 large-v3-turbo + dtw 词级时间戳，不再走 take 转录 + 剪辑表映射。
- 00:5x · whisper-cli `-ojf -dtw` 输出的 JSON 里 CJK 子词 token 是不完整的 UTF-8 字节，`json.load` 直接崩 → 用 `surrogateescape` 读入，按字节拼回完整字符。固化：时间线工具内置这一步。
- 01:15 · 即梦 CLI（dreamina text2video，seedance2.0 720p 5s）提交后排队位 16682 / 队列 253478，轮询 10 分钟仍在排队 → 今晚全部走 APIMart 兜底（同款 seedance-2.0，2 分钟一条）。固化：即梦只在队列 < 2000 时用；`dreamina list_task` 可事后收片替换。提交的 submit_id 58df8018-5463-49db-a1f5-1eb4bef0a4ff（40 积分），收片时对比质量。
- 01:20 · 分镜编译门禁抓到二分卡 8.4 秒无语义变化 → 加 pulseAt。规则有效。
- 01:40 · 给 APIMart 客户端加重试时正则把辅助函数内部的 `fetch(` 也替换成了自己 → 无限递归栈溢出，两批 B-roll 全空跑 20 分钟。根因：改网络客户端没做 smoke test。固化：`apimart-media.mjs` 增加 `--ping` 子命令（列模型）作为改动后的 10 秒自检；改客户端必跑。
- 01:55 · APIMart 余额耗尽（HTTP 402 payment_required）：6 条 B-roll 只出了 3 条，剩余 3 条失败；充值是资金操作，我不动。即梦队列 40 分钟只前进约 950 位（15730 / 252966 待排），今晚到不了。处理：缺片的 B-roll 槽位改成物件 / 标签实现，分镜里标注「待补 B-roll」，充值后一条命令补渲。固化：资产生成前先 `apimart-media.mjs ping` + 查余额（接口若无余额查询，就在日志里累计消费）。
- 02:35 · 员工片 v1：静帧 runner 36 张 32s（旧法 ~5 分钟）；几何门禁、词锚、语义门禁全过；渲染 runner 启动（8 并发）。
- 分镜 id 打字错（forget-broll vs forgotten-broll）导致漏删 → 编译门禁抓住。
- 03:39 · 渲染 runner 首跑失败：8 并发下 `<Img rulebook.png>` 的 delayRender 超过默认 28s 超时。固化：runner 传 `--timeout=120000`，失败自动降一半并发重试一次。
- 03:51 · 员工片 v1 出片：渲染 214s（102.5s 片，8 并发），门禁全绿，已交付 Downloads。runner 第二次运行一次通过。
- 03:55 · 自己的操作坑：同一条命令里 `cd` 进工作区后再用相对路径跑 runner → 找不到脚本。固化：runner 全部用绝对路径调用；说明书示例改成绝对路径。
- 04:05 · 独立站片静帧 48 张 42s；二分卡说明文字过长压到物件（场景内元素互压，几何门禁只查人物 vs 元素，抓不到）→ 缩短文案。固化候选：二分卡说明按可用宽度截断 / 缩字，或门禁扩到场景内元素两两求交。
- 04:17 · 独立站片 v1 出片：渲染 380s（182.3s 片，8 并发），门禁全绿，已交付 Downloads。
- 04:25 · 分层提交：引擎 / 工具 / 文档 / 共享库一笔（6f0d4db），四条片的分镜与 REVIEW 一笔。Codex 留下的十几个旧 job project.json 改动、reports/、skills/video-cover-factory 等未跟踪内容**未提交**，等 Scott 定。

## 收尾数字
- 员工片：102.5s，12 场，3 条 Seedance B-roll（6.6%），17 个物件，渲染 214s。
- 独立站片：182.3s，21 场（含循环形态首次上片、7 处真实 logo），14 个物件，渲染 380s。
- 缺口：3 条 B-roll 等 APIMart 充值；即梦排队未出片；两处官方页面证据（OpenAI 结账说明、Shopify Catalog）可补真实截图。
- 05:10 · Scott 看片：「整体不错，但有点八股」。诊断根因：一句配一画 + 4/6 秒节奏门禁逼出的均匀节拍、尺度/位置无跳跃、标签复述字幕。动作：6 秒门禁降为警告；新增人物特写推近（close）、镜像布局（hero-right）、巨字（giant）；两条片按「只留 4–6 个砸画面的点」重编，删复述标签。
- 05:15 · 几何门禁拦下巨字与人像重叠（人物切走晚了 0.5s）——门禁第一次在真实编辑里救场。编译失败留下无标记半成品工作区（第三次踩）→ 治理脚本加失败自清理。
- 14:20 · Scott 第二轮 review 纠正诊断：节奏没问题（可更激进），真实信息太少 + 口播无点缀 + 模板复用多且顺序雷同。落地：`accent` 点缀、`quote` 引用、`broll:strip` 通栏、字幕关键词加重、型录签名审计（跨 job 三元组重合 >35% 预警）、`scripts/web-evidence.sh` 抓真实网页（10 页抓 6 页，OpenAI/Amazon 反爬，换 developers.openai.com 成功）、每条片独立签名（背景板 / 开场 / 人物侧）。6 秒门禁恢复为硬门禁。
- 14:33 · Quote 首版把 blurSlide 返回值当成 {y,blur} 用（tsc 抓住）；引用 17 字在 980px 内折行出孤字 → 宽度 1080 + 字号阶梯。
- 14:39 · 员工片 v3 出片（真实页 ×3、引用、点缀、通栏开场、镜像判定、关键词字幕），渲染 240s，门禁全绿；Downloads 只留 v3。
- 14:48 · 独立站片 v3 出片（真实页 ×4、点缀 ×3、推近、镜像、巨字），渲染 419s，门禁全绿（两处突变均为证据页设计内的聚焦跳切）；Downloads 两条片只留 v3。
- 18:20 · Scott 第三轮 review（九条）：开场动画应表达"员工带走经验、留下智能体"；黑卡黑底；引用体错位；特写旁小字；GMV Max 用了不相关公开页（应问他要 GitHub 仓库）；流程图单调；英文页要中文；开场先正脸；多对一连用两遍；人物在左右边空。落地：卡片提亮、背景三道动态、`timeline` / `compare`、证据译注 `note`、开场正脸审计、同类模板轮替审计、短空闲延后切换、`MATERIAL-REQUEST.md` 四类判定。他要求先讨论再动手 → 出了选型页（背景 / 模板 / 未建模板）；v4 按现状渲成草稿，等他选背景与模板后重渲。
- 19:05 · Scott 否决四张背景板，要小Lin式立体透视：做成可切换皮肤 `plan.skin: stage3d`（网点/斜纹底 + 透视黑板 + 红/紫/青柠实底卡 + » 箭头 + 双语小字 + 青柠字幕关键词），六张样张进选型页。点缀改贴人物两侧肩高；引用按标点断行；人物居中时不画面板。
- 19:41 · Scott 三点：背景要微粒动态、面板要真 3D 不是垫图、面板要当容器且高亮反色。彻底修法：粒子层（120 颗三档景深呼吸）；面板绕 Y ±3° / X ±1.2° 慢摆 + 推拉，内容层前置 60px，元素各自分深度（节点按 id 三档、高亮前推 46px；阶梯 / 时间线 / 循环 / 对比 / 提醒同理）；容器门禁（stage3d 下任何元素超出面板 >8px 编译失败，首跑就抓到开场"员工滑出画面"越界）；高亮实底改纯青柠、文字色统一 ON_ACCENT 并加测试禁写死；整屏 / 通栏 B-roll 移出透视层。员工片 v6 出片给他看。
- 19:58 · Scott 四点：面板内间距挤、粒子应是点云波动 + 亮度呼吸、自带边框的内容别再套面板、荧光色不高级。改法：二分卡行框按面板内边距重排、面板留白加大（左右 48 / 上下 24）；canvas 点云（28px 网格两组慢波位移，波高定亮度与半径）取代静态网点 + 粒子；证据页 / B-roll / 数据卡出现时不画面板且容器门禁免检；配色收成哑光黄绿灰 #CDD27A、酒红 / 暗紫实底。员工片 v7。
- 21:10 · Scott：勾的模板必须全建并可路由，否则后面出问题。落地：title / chat / leaderboard / gauge / flip / map / clipping 七个组件 + stage 全屏舞台模式 + close-right/cut 双机位 + screen 录屏容器，全部进 Plan 类型、分发、编译器路由、几何登记、签名、注册表（22 种）；目录 job 27 场逐个渲通。覆盖率门禁上线首日抓到 4 处真实空白（目录树状图、员工片阶梯段、独立站 167s）；连续重复判定改按「类型:变体」签名。标题卡层级被全屏人物盖住、chat 靠边留白两个 bug 当场修。
- 21:40 · Scott 定名「xiaolin 模板」；硬流程：不明说要成片就先 `--proof` 540p 样片审，过了再 `--final` 1080p60 高码率打包 Downloads；旧模板归档；代码要可维护。落地：runner 双模式（60fps 靠合成帧率提升 + 逻辑帧插值，组件帧常量不动）、skill / 说明书改名与流程、「新增模板五步」+ 路由覆盖测试。
- 23:22 · 样片模式 smoke 通过（540p、不进 Downloads、门禁照跑）；60fps 探针：合成 60fps、120 帧 = 2.0s、动效插值正常；成片编码 8.9Mbps BT.709。测试 14 项全过。
